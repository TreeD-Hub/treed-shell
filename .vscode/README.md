# `.vscode`

Каталог с задачами запуска для локальной `tauri-only` разработки.

Содержимое:
- `tasks.json` — быстрый запуск `tauri:dev` и `tauri:build`.
- `launch.json` — пустой шаблон конфигураций отладки.
